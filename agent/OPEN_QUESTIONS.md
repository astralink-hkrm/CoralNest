# Open questions

<!-- Example:
## Convex schema field `packageRefs` on the skill table
Some skills reference package IDs (a skill could depend on a plugin).
Deferred: kept the field but stopped writing to it in M1, rather than a
destructive migration, since it's unclear if anything else reads it.
If the human wants a clean schema: needs a proper Convex migration to drop
the column.
-->
